services:
  editors:
    build: 
      context: .
      dockerfile: editors.Dockerfile
    ports: 
      - 8080:80

  client:
    build: 
      context: .
      dockerfile: client.Dockerfile

  server:
    build: 
      context: .
      dockerfile: server.Dockerfile