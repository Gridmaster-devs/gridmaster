import unittest


class TestTemplateForPipeline(unittest.TestCase):
    """Simple boilerplate class for unittests, intended to check
that the pipeline is indeed working."""

    def test_always_true(self) -> bool:
        self.assertTrue(True)

